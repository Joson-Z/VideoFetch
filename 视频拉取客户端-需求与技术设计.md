# 视频拉取客户端——需求与技术设计

> 文档状态：V0.1（开发前评审稿）  
> 目标平台：Windows 10/11 x64  
> 推荐技术栈：C# / .NET 8 / WPF + yt-dlp + FFmpeg  
> 首期目标站点：哔哩哔哩（B 站）

## 1. 项目目标

开发一个带图形界面的 Windows 桌面客户端。用户提供自己有权访问、下载和保存的视频地址，客户端读取用户当前登录状态，探测该账号实际可用的清晰度与音质，完成下载、音视频合并，并输出以视频标题命名的 MP4 文件。

核心流程：

```text
选择登录态 → 输入视频链接 → 获取视频信息与可用格式
→ 选择画质（默认当前账号可用的最高画质）
→ 选择最高可用音质 → 下载视频流和音频流
→ FFmpeg 自动合并/转封装 → 输出 MP4 → 用视频标题命名
```

本项目仅用于下载用户拥有版权、已获授权或平台明确允许离线保存的内容。不得绕过 DRM、付费权限、区域限制、风控或其他访问控制，不提供破解会员、盗取 Cookie、批量传播版权内容等功能。

## 2. 技术选型结论

### 2.1 推荐方案：C# 桌面客户端

| 层级 | 技术 | 用途 |
|---|---|---|
| UI | WPF + MVVM | Windows 原生桌面界面、数据绑定、任务列表 |
| 运行时 | .NET 8 | 进程管理、异步任务、配置与日志 |
| 视频解析/下载 | yt-dlp.exe | 登录态读取、元数据和格式探测、分段下载 |
| 音视频处理 | ffmpeg.exe / ffprobe.exe | 合并、转封装、必要时转码、结果校验 |
| 本地配置 | JSON | 保存非敏感设置，如输出目录、默认画质 |
| 敏感信息 | Windows DPAPI（仅在确需持久化时） | 加密 Cookie 文件路径或应用会话数据 |

选择 C# 的原因：

- 目标环境明确为 Windows，WPF 的发布、文件选择、任务栏进度和系统集成更直接。
- 可发布为自包含 x64 应用，用户无需单独安装 Python。
- yt-dlp 和 FFmpeg 均作为受控子进程运行，便于升级，也避免重复实现站点解析逻辑。
- 异步读取标准输出、暂停/取消任务、解析进度和错误码比较成熟。

Python + PySide6 也可以实现，但更适合快速原型；正式分发时包体、运行环境和杀毒软件误报处理通常更麻烦。除非团队现有代码主要是 Python，否则首版采用 C#。

## 3. 范围定义

### 3.1 V1 必须实现

- Windows 图形界面。
- 支持单条或多条 B 站视频链接输入。
- 支持读取 Edge、Chrome 的本机登录态。
- 支持导入 Netscape 格式 `cookies.txt`，作为浏览器读取失败时的备用方式。
- 显示登录状态：已登录/未登录/登录态失效/无法读取。
- 拉取视频标题、封面、作者、时长、分集信息和实际可下载格式。
- 画质下拉框只显示当前账号与当前视频确实可用的画质。
- 默认选择最高可用视频画质和最高可用音质。
- 下载分离的视频流和音频流，并自动合并。
- 最终输出 MP4，以视频标题命名。
- 支持下载目录选择、任务队列、实时进度、速度、预计剩余时间、取消和失败重试。
- Windows 非法文件名字符清理、重名处理和超长文件名截断。
- 日志中隐藏 Cookie、签名参数和临时媒体 URL。

### 3.2 建议在 V1.1 实现

- 内嵌 WebView2 扫码登录；登录域名白名单仅允许 B 站官方域名。
- 多 P 视频的分集选择。
- 合集/播放列表选择下载。
- 字幕、封面和元数据写入 MP4。
- 并发数、限速、代理设置。
- yt-dlp 与 FFmpeg 的校验和验证及一键更新。

### 3.3 明确不做

- 不收集或保存 B 站账号密码。
- 不提供绕过会员、付费、DRM、验证码、区域限制或平台风控的能力。
- 不保证第三方平台改版后永久可用；解析能力依赖 yt-dlp 的持续维护。
- 不把用户 Cookie 上传到服务器；V1 完全本地运行。

## 4. 登录态设计

### 4.1 登录方式优先级

1. **从浏览器读取（默认）**：用户选择 Edge 或 Chrome，并选择浏览器配置文件；客户端调用 yt-dlp 的 `--cookies-from-browser`。
2. **导入 cookies.txt（备用）**：用户自行提供 Netscape/Mozilla 格式 Cookie 文件。
3. **WebView2 扫码登录（后续）**：应用内打开 B 站官方登录页，扫码完成后只在本机使用对应站点 Cookie。

界面不提供“账号”和“密码”输入框。原因是平台可能存在验证码、二次验证和风控，直接代收密码既不可靠也扩大安全风险。界面中的“登录信息”应表示浏览器、浏览器配置文件或 Cookie 文件。

### 4.2 登录状态校验

用户点击“检测登录状态”或粘贴链接后，程序执行一次只读取元数据、不下载媒体的探测：

```powershell
yt-dlp.exe --dump-single-json --skip-download --no-warnings `
  --cookies-from-browser edge "<VIDEO_URL>"
```

判断规则：

- 能读取视频元数据且返回账号相关状态：显示“登录有效”。
- 能读取公开视频但没有登录身份：显示“未登录，可用画质可能受限”。
- Cookie 解密失败或浏览器数据库被占用：显示可操作的错误提示，建议关闭浏览器重试或导入 `cookies.txt`。
- 视频无权访问：区分登录失效、内容不存在、地区限制、会员/付费权限不足等情况；只展示错误，不尝试绕过。

### 4.3 敏感数据要求

- 默认每次直接从浏览器读取，不落盘复制 Cookie。
- 导入 Cookie 文件时只保存路径；如需复制到应用目录，必须获得用户明确选择，并使用当前 Windows 用户范围的 DPAPI 加密。
- 日志不得记录 Cookie 内容、请求头、临时下载地址或完整的调试命令。
- 崩溃报告发送前必须清理敏感字段。
- 提供“清除登录信息”按钮，删除应用保存的会话信息和临时文件。

## 5. 界面设计

### 5.1 主窗口布局

```text
┌─────────────────────────────────────────────────────────────┐
│ 视频拉取客户端                                  设置  关于  │
├─────────────────────────────────────────────────────────────┤
│ 登录方式  [Edge ▼]  配置文件 [Default ▼] [检测登录状态]    │
│ 状态：● 已登录 / ● 未登录 / ● 登录态失效                    │
├─────────────────────────────────────────────────────────────┤
│ 视频地址（每行一个，也支持直接粘贴）                         │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ https://www.bilibili.com/video/BV...                   │ │
│ └─────────────────────────────────────────────────────────┘ │
│ [解析视频]                                                  │
├─────────────────────────────────────────────────────────────┤
│ 封面  标题：……                                              │
│       作者：……   时长：……   分集：[全部/选择…]              │
│ 画质：[自动：最高可用 ▼]  音质：[自动：最高可用 ▼]          │
│ MP4 模式：[保留最高源质量 ▼]                                │
│ 保存到：[D:\Videos\________________________] [浏览…]       │
│ [加入队列] [立即下载]                                       │
├─────────────────────────────────────────────────────────────┤
│ 下载队列                                                    │
│ 42%  标题……  8.2 MiB/s  剩余 00:36            [取消]       │
│ 等待  标题……                                                │
├─────────────────────────────────────────────────────────────┤
│ 状态/精简日志                                                │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 用户操作流程

1. 首次启动检查 `yt-dlp.exe`、`ffmpeg.exe`、`ffprobe.exe` 是否存在且可执行。
2. 用户选择 Edge/Chrome 登录态或导入 Cookie 文件。
3. 用户粘贴一个或多个视频地址。
4. 客户端解析元数据和格式列表。
5. 客户端显示当前账号能拿到的画质和音质，默认选最高值。
6. 用户选择保存目录并开始下载。
7. UI 分别展示视频流、音频流、合并/转封装阶段的进度。
8. 完成后用 ffprobe 校验文件，显示“打开文件”和“打开目录”。

## 6. 画质与音质选择

### 6.1 解析可用格式

解析阶段使用 JSON，而不是解析命令行的自然语言文本：

```powershell
yt-dlp.exe --dump-single-json --skip-download `
  --cookies-from-browser edge "<VIDEO_URL>"
```

读取 JSON 中的 `formats`，规范化为应用内部模型：

```csharp
public sealed record MediaFormat(
    string FormatId,
    int? Width,
    int? Height,
    double? Fps,
    string? VideoCodec,
    string? AudioCodec,
    double? VideoBitrate,
    double? AudioBitrate,
    string? Extension,
    long? FileSize,
    bool HasVideo,
    bool HasAudio);
```

同一分辨率可能存在 AVC/H.264、HEVC/H.265、AV1，不应只按 `height` 判断“最好”。默认排序建议：

1. 分辨率。
2. 帧率。
3. 动态范围/HDR 信息。
4. 视频总码率或估算码率。
5. 编码偏好；“保留最高源质量”不强行限制编码。

音频默认选择所有可用音频流中的最高质量项，主要比较音频码率与编码信息。最终以 yt-dlp 返回的可下载格式为准，不在 UI 中展示账号无权获取的选项。

### 6.2 画质选项

- 自动：最高可用（默认）。
- 8K / 4K / 1080P 高帧率 / 1080P / 720P / 480P / 360P：仅在探测到时显示。
- “不超过所选画质”：例如选择 1080P 时，在 `height <= 1080` 范围内选择最佳视频流。
- 高帧率、HDR、杜比视界等能力应作为格式标签显示，不凭分辨率猜测。

建议的 yt-dlp 格式表达式：

```text
最高可用：bv*+ba/b
不超过 2160P：bv*[height<=?2160]+ba/b[height<=?2160]
不超过 1080P：bv*[height<=?1080]+ba/b[height<=?1080]
不超过 720P： bv*[height<=?720]+ba/b[height<=?720]
```

实际下载前应使用本次解析得到的格式 ID 或受限表达式，并重新验证格式仍然可用。格式 ID 属于站点/视频相关数据，不可硬编码到程序中。

## 7. MP4 输出策略

“最高源画质”和“最广泛的 MP4 播放兼容性”不是完全相同的目标。最高画质可能使用 HEVC、AV1 或特殊音频编码；强制转成 H.264/AAC 会增加耗时，并发生一次有损转码。因此界面提供两种模式：

### 7.1 保留最高源质量（默认）

- 选择最高质量的视频流和音频流。
- 优先用 FFmpeg 转封装/流复制到 MP4，不重新编码。
- 如果流无法放入 MP4，则进入兼容性回退；UI 明确显示正在转码。
- 优点：最大限度保留源质量，通常速度快。
- 注意：较老的播放器可能无法解码 MP4 中的 HEVC/AV1。

示意命令：

```powershell
yt-dlp.exe "<VIDEO_URL>" `
  --cookies-from-browser edge `
  -f "bv*+ba/b" `
  --merge-output-format mp4 `
  --remux-video mp4 `
  --windows-filenames `
  --trim-filenames 180 `
  -P "<OUTPUT_DIRECTORY>" `
  -o "%(title)s.%(ext)s" `
  --newline --progress --no-playlist
```

### 7.2 通用兼容 MP4

- 优先选择 H.264/AVC 视频和 AAC/M4A 音频中满足目标分辨率的最佳流。
- 如果没有兼容流，下载所选最高质量源，再由 FFmpeg 转码为 H.264 + AAC。
- 优点：兼容多数电视、手机和编辑软件。
- 缺点：可能不是平台提供的绝对最高编码版本；回退转码耗时且会带来代际损失。

转码回退示意：

```powershell
ffmpeg.exe -i "<INPUT>" -map 0:v:0 -map 0:a:0? `
  -c:v libx264 -preset medium -crf 18 `
  -c:a aac -b:a 192k -movflags +faststart "<OUTPUT>.mp4"
```

命令行仅为设计示意。实现时必须使用 `ProcessStartInfo.ArgumentList` 逐项传参，禁止拼接后交给 shell 执行，避免标题、目录或 URL 中特殊字符导致命令注入。

## 8. 文件命名与冲突处理

默认模板：

```text
%(title)s.%(ext)s
```

规则：

- 最终扩展名固定为 `.mp4`。
- 替换 Windows 禁止字符：`< > : \ / | ? *` 和控制字符。
- 去除末尾空格与句点，规避 Windows 保留名称。
- 标题建议最多 180 个字符，为目录前缀和扩展名保留空间。
- 同名文件默认追加 ` (1)`、` (2)`；设置页可改为“覆盖”“跳过”或“附加 BV 号”。
- 多 P 视频建议命名为：`标题 - P01 - 分P标题.mp4`。
- 合集建议建立以合集标题命名的子目录。

## 9. 系统架构

```text
WPF View
  │
  ▼
ViewModel / Application Services
  ├── LoginSessionService       登录方式与登录态探测
  ├── MediaProbeService         元数据、分集、格式解析
  ├── FormatSelectionService    画质/音质筛选与选择
  ├── DownloadQueueService      排队、并发、取消、重试
  ├── YtDlpProcessRunner        安全启动 yt-dlp 与解析进度
  ├── FfmpegService             合并、转封装、转码
  ├── MediaValidationService    ffprobe 结果校验
  ├── FileNameService           文件名清洗与冲突处理
  ├── ToolchainService          依赖检查、版本信息、更新
  └── Settings/Logging          配置、脱敏日志
```

### 9.1 推荐项目结构

```text
VideoFetch.sln
src/
  VideoFetch.App/                 WPF、View、ViewModel、资源
  VideoFetch.Application/         用例、接口、队列和业务规则
  VideoFetch.Domain/              任务、媒体格式、枚举、结果模型
  VideoFetch.Infrastructure/      yt-dlp、FFmpeg、文件系统、配置
tests/
  VideoFetch.UnitTests/
  VideoFetch.IntegrationTests/
tools/
  yt-dlp.exe
  ffmpeg.exe
  ffprobe.exe
docs/
```

### 9.2 核心任务状态

```text
Created → Probing → Ready → Queued → DownloadingVideo/Audio
→ Merging → Remuxing/Transcoding → Validating → Completed

任意运行态 → Cancelling → Cancelled
任意运行态 → Failed → Retrying/Queued
```

一个任务需要独立的 `CancellationTokenSource`。取消时先请求子进程正常退出，超时后再终止该任务的进程树，并清理该任务创建的临时分片，不能影响其他任务。

## 10. 子进程与进度解析

### 10.1 启动要求

- 使用 `ProcessStartInfo.UseShellExecute = false`。
- 使用 `ArgumentList` 添加参数，不拼接 shell 命令。
- 同时异步读取标准输出和标准错误，避免缓冲区阻塞。
- 所有进程设置任务 ID，日志按任务归档。
- 通过 `--newline` 获得逐行进度；后续可使用 yt-dlp 的 `--progress-template` 输出固定格式字段。
- 退出码为 0 仍需检查目标文件存在，并用 ffprobe 校验。

### 10.2 进度阶段

总进度不能只照搬下载百分比，建议按阶段加权：

| 阶段 | 默认权重 |
|---|---:|
| 元数据与格式探测 | 2% |
| 媒体下载 | 88% |
| 合并/转封装/转码 | 8% |
| ffprobe 校验与收尾 | 2% |

转码耗时不可准确预估时，显示 FFmpeg 已处理时间/总时长，而不是假装下载已完成。

## 11. 结果校验

下载成功后调用 ffprobe 读取 JSON：

```powershell
ffprobe.exe -v error -print_format json -show_format -show_streams "<OUTPUT.mp4>"
```

完成条件：

- 文件存在且大小大于 0。
- 容器可被 ffprobe 读取。
- 至少存在一个视频流。
- 对非静音源，存在音频流。
- 视频时长与解析阶段元数据在允许误差范围内。
- 扩展名为 `.mp4`，文件名符合命名规则。

校验失败时保留可诊断日志；默认不删除已下载的源流，允许用户重试合并。用户确认或重试成功后再清理临时文件。

## 12. 错误处理与提示

| 场景 | 用户提示 | 程序动作 |
|---|---|---|
| yt-dlp/FFmpeg 缺失 | 缺少组件，提供安装/选择路径入口 | 禁止开始任务 |
| 浏览器 Cookie 无法读取 | 建议关闭浏览器重试或导入 Cookie 文件 | 不记录 Cookie 细节 |
| 未登录/登录过期 | 重新在浏览器登录后检测 | 允许继续下载公开可用画质 |
| 当前账号无该画质权限 | 自动降到实际可用最高画质并提示 | 不绕过权限 |
| 视频不存在/已下架 | 视频不可访问 | 任务失败，不重试 |
| 网络中断 | 网络异常，将按策略重试 | 指数退避，最多 3 次 |
| 磁盘空间不足 | 显示所需与可用空间 | 下载前拦截 |
| 合并失败 | 保留临时流并给出重试入口 | 不重复下载已完成流 |
| 输出文件重名 | 按用户设置跳过/覆盖/追加序号 | 默认追加序号 |

错误提示分两层：主界面显示简洁结论与解决动作；“查看详情”显示已脱敏的技术日志。

## 13. 依赖与发布

### 13.1 工具依赖

- `yt-dlp.exe`：负责站点适配、登录态读取、格式选择与下载。
- `ffmpeg.exe`：负责音视频合并、转封装和必要时转码。
- `ffprobe.exe`：负责输出校验。

应用启动时记录工具版本，并允许用户手动检查更新。更新包应从项目官方发布渠道获取，并验证 SHA-256 或发布签名。不能在后台静默替换二进制文件。

### 13.2 发布方式

- `.NET 8` self-contained、win-x64。
- 首版可发布为 ZIP 绿色版，后续增加 MSIX/安装包。
- 应用本体、工具链、配置、日志和临时下载目录分开。
- 卸载时默认保留用户已下载视频；是否删除配置和日志由用户选择。

## 14. 测试计划

### 14.1 单元测试

- 文件名非法字符、保留名称、超长与重名处理。
- 格式列表排序：分辨率、帧率、码率和编码组合。
- 画质上限选择及缺失字段处理。
- yt-dlp 固定格式进度行解析。
- Cookie、URL 查询参数和请求头的日志脱敏。
- 任务状态转换和取消逻辑。

### 14.2 集成测试

- 未登录的公开视频。
- Edge 已登录与 Chrome 已登录。
- 登录失效、Cookie 文件损坏、浏览器数据库占用。
- 音视频分离、单文件格式、无音轨视频。
- 360P、720P、1080P、高帧率、4K（测试账号有权访问时）。
- AVC、HEVC、AV1 视频和不同音频编码。
- 多 P、中文/英文/Emoji/特殊字符标题。
- 网络中断、磁盘不足、用户取消和合并失败后重试。

测试素材必须为团队自有、已授权或平台提供的公开测试内容，自动化测试不使用真实个人账号 Cookie。

## 15. V1 验收标准

以下条件全部满足才视为 V1 完成：

1. 在 Windows 10/11 x64 上可正常启动，无需用户安装 Python。
2. 可从 Edge 或 Chrome 读取当前登录态，也可导入规范 Cookie 文件。
3. 输入合法 B 站链接后，能显示标题和当前账号实际可用画质。
4. 默认选择最高可用视频与最高可用音频。
5. 用户选择 1080P 等上限时，结果不超过所选值，并在范围内选择最佳格式。
6. 音视频分离时可自动调用 FFmpeg 合并。
7. 最终生成可被 ffprobe 正常识别的 MP4 文件。
8. 文件默认以视频标题命名，并正确处理 Windows 非法字符和同名冲突。
9. UI 能显示任务状态、进度、速度、剩余时间，并支持取消和失败重试。
10. 日志及 UI 不泄露 Cookie、临时媒体 URL或其他登录凭据。
11. 无权限内容只给出明确提示，不尝试绕过平台限制。

## 16. 开发阶段建议

### 阶段 A：命令行可行性验证

- 固定一个已授权测试视频。
- 验证 Edge/Chrome 登录态读取。
- 验证格式 JSON、最高画质选择、MP4 合并和 ffprobe 校验。
- 固化一组脱敏后的 JSON 样本，用于后续单元测试。

### 阶段 B：核心库

- 实现进程运行器、媒体探测、格式选择、文件名清洗和结果校验。
- 先用单任务串行流程打通，不急于做复杂 UI。

### 阶段 C：WPF 界面

- 实现登录区、链接输入、媒体信息、画质选择和任务进度。
- 引入任务队列、取消、重试与设置持久化。

### 阶段 D：发布与加固

- 完成依赖检查、脱敏日志、异常恢复和安装包。
- 在干净的 Windows 10/11 虚拟机中完成验收。

## 17. 主要风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| B 站页面/API 变化 | 解析或下载阶段失效 | 与 yt-dlp 解耦，支持独立更新和版本回退 |
| 浏览器 Cookie 加密/占用 | 无法读取登录态 | 提示关闭浏览器、选择配置文件或导入 Cookie |
| 最高编码与 MP4 播放器不兼容 | 文件生成但部分设备不能播 | 提供“保留源质量”和“通用兼容”两种模式 |
| 强制转码耗时 | CPU 占用高、处理时间长 | 默认优先转封装，转码前明确提示 |
| 标题导致路径非法/过长 | 下载完成但无法落盘 | 统一 FileNameService，下载前生成最终路径 |
| Cookie 泄露 | 账号安全风险 | 不收密码、不上传 Cookie、DPAPI、日志脱敏 |
| 版权或平台规则风险 | 不当使用 | 权限提示、用途声明、不实现绕过控制能力 |

## 18. 参考资料

- yt-dlp 官方项目与参数说明：<https://github.com/yt-dlp/yt-dlp>
- yt-dlp Cookie FAQ：<https://github.com/yt-dlp/yt-dlp/wiki/FAQ>
- FFmpeg 官方命令文档：<https://ffmpeg.org/ffmpeg.html>

---

## 19. 待确认项

进入编码前建议确认以下产品决策：

1. V1 是否只支持 B 站，还是界面文案和模型从一开始就为多站点预留。
2. 默认 MP4 模式是“保留最高源质量”还是“通用兼容”。本文建议前者，以符合“能拿到的最高画质”。
3. V1 是否需要多 P/合集，还是先完成单视频闭环。
4. 工具链是随安装包内置，还是首次启动后由用户选择本机已有文件。
5. 是否允许保存 Cookie 文件副本；本文建议默认不允许，仅保存来源设置。

